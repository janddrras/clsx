<div class="container">
  <div class="banner">
    <div class="text">
      <p>Welcome to <span>CLSX!</span> This is a fake website inspired by HQX 21.</p>
      <p>Welcome to <span>CLSX!</span> This is a fake website inspired by HQX 21.</p>
      <p>Welcome to <span>CLSX!</span> This is a fake website inspired by HQX 21.</p>
    </div>
  </div>
</div>

<style>
  .banner {
    overflow: hidden;
    width: 100%;
    position: relative;
  }
  .text {
    padding: 1rem 0;
    white-space: nowrap;
    color: white;
    display: inline-block;
    font-size: 2.5rem;
    font-weight: 400;
    animation: move 25s linear infinite;
  }
  .text p {
    padding: 0 0.5rem;
    display: inline-block;
  }
  span {
    font-weight: 700;
  }
  .banner::before,
  .banner::after {
    content: "";
    position: absolute;
    top: 0;
    width: 25%;
    height: 100%;
    z-index: 2;
  }
  .banner::after {
    background: linear-gradient(to left, var(--black), transparent);
    right: 0;
  }
  .banner::before {
    left: 0;
    background: linear-gradient(to right, var(--black), transparent);
  }
  @keyframes move {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }
</style>
